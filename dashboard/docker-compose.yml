services:
  airport-app:
    build: .
    container_name: airport-dashboard
    ports:
      - "11501:8501"
    volumes:
      - .:/app
    env_file:   
      - ./.env
    networks:
      - pan_web
    restart: always

networks:
  pan_web:
    driver: bridge